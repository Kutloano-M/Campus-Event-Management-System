# Campus-Event-Management-System
Console Java app for campus events with Staff (create, update, cancel, view participants/waitlists) and Students (view, register, cancel, view status). Events have unique ID, name, date/time, location, capacity, registered list, waitlist. First waitlisted student auto-promoted on cancellation via background thread. Data persisted, inputs validated.
